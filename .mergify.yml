pull_request_rules:
  - name: vm5 sibling review semantic gap
    conditions:
      - base = main
      - title = VM5 sibling review semantic gap target
    actions:
      merge:
        method: squash
