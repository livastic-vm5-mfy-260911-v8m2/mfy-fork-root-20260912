commands_restrictions:
  queue:
    conditions:
      - sender-permission >= write
queue_rules:
  - name: default
    queue_conditions: []
    merge_conditions:
      - check-success = vm5-reviewcmd-never
    batch_size: 1
    batch_max_wait_time: 0
    merge_method: merge
pull_request_rules: []
