pull_request_rules:
  - name: delete external fork head after merge
    conditions:
      - merged
      - from-fork
    actions:
      delete_head_branch:
  - name: positive same-repo delete after merge
    conditions:
      - merged
      - -from-fork
    actions:
      delete_head_branch:
