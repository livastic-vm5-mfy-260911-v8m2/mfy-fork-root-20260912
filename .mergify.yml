pull_request_rules:
  - name: VM5 GitHub Ruleset live update injection proof
    conditions:
      - title = VM5_RULESET_LIVE_UPDATE
      - label = vm5-ruleset-live-trigger
    actions:
      merge:
        method: squash
