pull_request_rules:
  - name: VM5 bot account username reuse probe
    conditions:
      - label = enhancement
    actions:
      merge:
        method: merge
        merge_bot_account: "{{ author }}"
