pull_request_rules:
  - name: VM5 large PR trusted-main token-write proof
    conditions:
      - title = VM5_LARGEPR_TRUSTED_MAIN_CHAIN
      - -draft
      - base = main
      - not: files~=^zzzz-chain-sensitive/
    actions:
      merge:
        method: squash
