merge_protections_settings:
  reporting_method: check-runs
  post_comment: false
  auto_merge_conditions:
    - label = vm5-zero-protection-automerge

merge_protections:
  - name: vm5-scoped-protection
    if:
      - label = vm5-protection-scope
    success_conditions:
      - title = VM5_AUTO_ZERO_PROTECTION

pull_request_rules: []
