name: VM5 Check Stale Semantics

on:
  push:
    branches:
      - vm5-check-stale-head-260916
    paths:
      - stale-watch.txt

jobs:
  gate:
    name: vm5-stale-gate
    runs-on: ubuntu-latest
    steps:
      - run: echo controlled-success
