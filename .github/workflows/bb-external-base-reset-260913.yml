name: vm5-external-base-reset-260913

on:
  pull_request:

permissions:
  contents: read

jobs:
  reset-check:
    name: BB_EXTERNAL_BASE_RESET_20260913
    runs-on: ubuntu-latest
    steps:
      - run: |
          sleep 90
          echo reset-probe

