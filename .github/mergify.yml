merge_protections:
  - name: FALLBACK_ALWAYS_ALLOW_CONTROL
    if: true
    success_conditions: true
merge_protections_settings:
  auto_merge_conditions:
    - -mergify-configuration-changed
pull_request_rules:
  - name: FALLBACK_CLASSIFIER_RUNTIME_PROBE
    conditions:
      - -mergify-configuration-changed
      - base = main
    actions:
      comment:
        message: VM5_FALLBACK_CLASSIFIER_FALSE_RUNTIME
  - name: FALLBACK_BASE_MARKER
    conditions:
      - base = main
    actions:
      comment:
        message: FALLBACK_BASE_CONFIG_ACTIVE
  - name: VM5_FALLBACK_ATTACKER_POLICY2
    conditions:
      - title = VM5_FALLBACK_POSTMERGE_POLICY_PROBE2
    actions:
      comment:
        message: VM5_FALLBACK_ATTACKER_POLICY2_ACTIVE
